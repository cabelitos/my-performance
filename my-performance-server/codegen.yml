overwrite: true
schema: "./src/schemas/schema.graphql"
documents: null
config:
  scalars:
    Date: Date
  nonOptionalTypename: false
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
