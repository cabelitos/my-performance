overwrite: true
schema: "./src/schemas/index.ts"
documents: null
config:
  scalars:
    Date: Date
  nonOptionalTypename: false
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
